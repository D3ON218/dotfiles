general {
    no_fade_in = false
    grace = 0
    disable_loading = false
}

background {
    monitor =
    # Asegúrate de poner la ruta a tu imagen de la estatua aquí
    path = /home/oswaldo/Pictures/estatua_vaporwave.png 
    color = rgba(25, 20, 20, 1.0)
    blur_passes = 1
}

input-field {
    monitor =
    size = 250, 50
    outline_thickness = 3
    dots_size = 0.2
    dots_spacing = 0.15
    dots_center = true
    outer_color = rgb(ff71ce) # Rosa Neón
    inner_color = rgb(c0c0c0) # Gris Retro
    font_color = rgb(05ffa1)  # Cian Neón
    fade_on_empty = false
    placeholder_text = <span foreground="##05ffa1">Contraseña...</span>
    position = 0, -20
    halign = center
    valign = center
}

label {
    monitor =
    text = SISTEMA_BLOQUEADO // USER: OSWALDO
    color = rgb(ff71ce)
    font_size = 22
    font_family = Sans # Luego podemos cambiarla a una pixelada
    position = 0, 80
    halign = center
    valign = center
}